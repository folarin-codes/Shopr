import SafeView from "@/component/safeview";
import { Text } from "react-native";

const Saved = ()=>{
    return(
        <SafeView>
            <Text>Saved</Text>

        </SafeView>

    )
}


export default Saved;